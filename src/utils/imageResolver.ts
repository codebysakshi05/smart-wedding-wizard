/**
 * imageResolver.ts
 * ================
 * Central re-export hub for all image resolver functions.
 * Uses strictAssetResolver which references the manifest for correct file paths.
 */

export * from "./strictAssetResolver";
